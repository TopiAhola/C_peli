///////////////////////////////////////////////////
TODO:

Learn to read memory and debug by stepping.

Replace Directsound with Xaudio

Document build environment with pictures.

Get proper gamepad and test xinput.

Sound latency needs to influence image flip. And frame time target needs to set sound write location in buffer!




///////////////////////////////////////////////////
Done:

Korjaa OutputDebugString debuggausta varten...

Key up shouldn't count as input for controlling things? Otherwise liftin a key stalls other keypresses.
Or specify priority for inputs? (Fixed))

Change sound write to just before the read cursor.








///////////////////////////////////////////////////
Not relevat:

KB input should be written to an array with timestamps and passed in to game function. Or some other way that enables higher physics tickrate
